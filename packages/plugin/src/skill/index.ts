/**
 * @saulene/plugin — skill
 *
 * The optional `/ul` command: "show my ul" — view its current MBTI readout, aspects
 * (0–100 display scale), stage, age, mood, and arc. Manual ops.
 */

// TODO(plugin): /ul command surface.

export {};
