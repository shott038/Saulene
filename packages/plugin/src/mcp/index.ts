/**
 * @saulene/plugin — mcp
 *
 * The MCP core: the soul's state + identity + registry/on-chain tools — the portable,
 * standard-shaped box. Extractable so other hosts can connect the bare MCP (degraded,
 * manual: no hooks). Holds/serves data; it does NOT run the life (hooks do).
 */

// TODO(plugin): MCP server (@modelcontextprotocol/sdk) exposing soul state/identity tools.

export {};
