/**
 * @saulene/core — birth
 *
 * Birth seeding: research-grounded set points (Big Five Aspect Scale distributions,
 * Weisberg/DeYoung/Hirsh 2011) — per-aspect Gaussians, gender-shifted means, 50/50 sex,
 * random stubborn↔clay position. Produces a fresh Soul from injected entropy.
 *
 * PURE on purpose: takes entropy bytes as an argument (the plugin supplies real
 * randomness). Same entropy in → same soul out, so births are replayable.
 */

// TODO(core): SEEDING_SIGMA per aspect; GENDER_D per aspect.
// TODO(core): seedFromEntropy(entropy: Uint8Array, now: number): Soul

export {};
