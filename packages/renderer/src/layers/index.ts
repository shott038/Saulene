/**
 * @saulene/renderer — layers
 *
 * The 5-layer renderer stack (each layer covers another's fatal flaw):
 *   1. Behavioral-directive rulebook (the floor; works day-1 from the 10 numbers)
 *   2. State-matched real-voice few-shot (the pervade engine)
 *   3. Spine / disposition (what makes it FELT, not a costume)
 *   4. Framing + anti-decay wrapper
 *   5. Drift, shown not told
 *
 * Each layer is a pure fragment producer so the harness can ablate one aspect at a time.
 */

// TODO(renderer): one module per layer; assemble() stacks them.

export {};
