/**
 * @saulene/renderer — fingerprint
 *
 * Stylometric backstop: function-word rates, sentence-length distribution, punctuation
 * tics — recomputed incrementally from real samples (cheap regex, no model calls).
 * A deterministic guard that keeps the voice from drifting toward generic-assistant prose.
 */

// TODO(renderer): computeFingerprint(samples) — incremental stylometrics.

export {};
